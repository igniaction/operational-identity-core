services:

  postgres:

    image: postgres:16

    container_name: operational_identity_postgres

    environment:
      POSTGRES_DB: identity_core
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"
